    //inline(em linha)

/*
    inblock (em bloco)
*/
    //incluir biblioteca de entrada e saida padrão(stdio)
#include <stdio.h>
    //função main(principal) responsavel pela execulção do arquivo 
int main (){ 
    //comando printf , exibe a mensagen hello word na tela
    printf("hello world!\n");//(n)quebra de linha 
    //retornoda função main que foi declarada com int
    return 0;
}